# History

```shell
ng new angular-starter --directory=. --routing --style scss --ssr false --zoneless false
```

```shell
ng add @ng-bootstrap/ng-bootstrap --skip-confirmation
ng generate environments
```

```shell
ng g c -s pages/index
ng g c -s pages/login
```